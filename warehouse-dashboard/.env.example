# Copy this file to .env and adjust for your environment.

# Where the real-time warehouse backend (serverBackend.js) lives.
WAREHOUSE_BACKEND_URL=http://10.1.100.47:4000

# Port this dashboard itself listens on when run standalone (server.js).
# Not used when mounted as a route inside another app.
PORT=4500
